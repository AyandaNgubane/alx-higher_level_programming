#!/usr/bin/python3

def best_score(a_dictionary):
    if a_dictionary:
        new = dict(sorted(a_dictionary.items()))
        for best in new:
            continue
        return best
    return a_dictionary
