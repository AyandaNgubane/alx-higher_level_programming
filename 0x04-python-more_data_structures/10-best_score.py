#!/usr/bin/python3

def best_score(a_dictionary):
    if a_dictionary:
        return (max(a_dictionary, val = a_dictionary.get))
        '''
        new = dict(sorted(a_dictionary.items()))
        for best in new:
            continue
        return best
        '''
    return None
